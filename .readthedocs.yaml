version: 2
sphinx:
  configuration: docs/source/conf.py

formats:
  - pdf

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"
  apt_packages:
    - libopenmpi-dev

python:
  install:
    - requirements: docs/requirements_doc.txt
    - method: pip
      path: .
